  GNU nano 9.0           .github/workflows/android-apk.yml
# mobile/.github/workflows/android-apk.yml
name: Build Expo APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

                 [ Read 39 lines (converted from DOS format) ]
^G Help      ^O Write Out ^F Where Is  ^K Cut       ^T Execute   ^C Location
^X Exit      ^R Read File ^\ Replace   ^U Paste     ^J Justify   ^/ Go To Line
# mobile/.github/workflows/android-apk.yml
name: Build Expo APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install
        working-directory: mobile

      - name: Install Expo CLI
        run: npm install -g expo-cli eas-cli

      - name: Login to Expo
        run: eas login --username ${{ secrets.EXPO_USERNAME }} --password ${{ secrets.EXPO_PASSWORD }}

      - name: Build APK
        run: eas build --platform android --non-interactive --profile preview
        working-directory: mobile

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChargeUP-APK
          path: mobile/dist/*.apk
